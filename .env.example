APP_ENV=development
LOG_LEVEL=INFO
CONFIG_DIR=config
DATA_DIR=data
REPORTS_DIR=reports
LOGS_DIR=logs

# Phase 1 requires no broker credentials.
# Never commit API keys, access tokens, or private keys.
